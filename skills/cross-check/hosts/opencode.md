# OpenCode host

No `$ARGUMENTS`. Use the current user message.

Spawn researcher and frame-challenger with `debate-research`. Spawn the synthesizer with `debate-editor`.

Do not pass `sonnet` / `opus`. Wait for child tasks the OpenCode way. See `opencode.json`.
