# OpenCode host

No `$ARGUMENTS`. Use the current user message.

Spawn the analyst, researcher, and frame-challenger with `debate-research` or `debate-worker`. Spawn the reviser with `debate-editor`.

Do not pass `sonnet` / `opus`. Wait for child tasks the OpenCode way. See `opencode.json` and `skills/debate/hosts/opencode.md`.
